# KB 1 — period number -> clock time. v7 §3 strategy: fill BROADLY but only
# where it has leverage. Key = "<school>" or "<school>_<campus>" (campus matters:
# 원주 differs from 서울, SYL-024).
#
# MEASURED (2026-07, corpus 1026 docs): only 7 docs (0.7%) use 교시 notation at
# all — the schools below cover 100% of them. Fill THESE five and stop; the
# long tail resolves as needs_review by design (v7 §1, §4).
#
# HUMAN TODO (est. <1h): fill `periods` from each school's official timetable.
# Format per entry:  "1": ["09:00", "09:50"]
timetables:
  yonsei_seoul:      # 연세대 (신촌/국제 — YISS docs)
    periods: {}
  konkuk:            # 건국대
    periods: {}
  ewha:              # 이화여대
    periods: {}
  hongik:            # 홍익대
    periods: {}
  dongguk:           # 동국대
    periods: {}

  # --- format example (synthetic values; replace or remove) ---
  _example__regular:
    period_length_min: 50
    periods:
      "1": ["09:00", "09:50"]
      "2": ["10:00", "10:50"]
      "3": ["11:00", "11:50"]
